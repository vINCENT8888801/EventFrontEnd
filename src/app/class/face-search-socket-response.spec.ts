import { FaceSearchSocketResponse } from './face-search-socket-response';

describe('FaceSearchSocketResponse', () => {
  it('should create an instance', () => {
    expect(new FaceSearchSocketResponse()).toBeTruthy();
  });
});
